*git*
git
    $ git pull --rebase # rebase prevents merge commits
    $ git stash; git stash pop
    $ git reset HEAD^; git push -f # resets last commit in local repo and remote
    $ git merge --abort # aborts merge status
    $ git reset --hard origin # sets head to origin after diverged paths
